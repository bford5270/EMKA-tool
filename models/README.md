# models/ (gitignored)

Model weights staged at provisioning time (`make models`), never committed.
The exact required file list is written here by Prompt 2. All models are
config-driven paths (core/config.py) — dev uses small variants, the x86
target uses larger ones, same code.
